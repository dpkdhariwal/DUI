{
  "name": "root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --parallel",
    "build": "lerna run build --scope duilibb",
    "dev-lib": "lerna run dev --scope duilibb",
    "docs:dev":"lerna run docs:dev --scope duilibb",
    "dev-vue": "lerna run dev --scope vue-project"
  },
  "devDependencies": {
    "lerna": "^8.2.0"
  },
  "dependencies": {
    "duilibb": "^0.0.3",
    "i": "^0.3.7",
    "npm": "^11.1.0"
  }
}
