{
  "name": "root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --parallel",
    "build": "lerna run build --scope library",
    "dev-lib": "lerna run dev --scope library"
  },
  "devDependencies": {
    "lerna": "^8.2.0"
  },
  "dependencies": {
    "duilibb": "^0.0.2",
    "i": "^0.3.7",
    "npm": "^11.1.0"
  }
}
